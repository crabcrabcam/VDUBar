project 'VoiceBar.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VoiceBar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VoiceBar
  pod 'AudioKit', '~> 3.5'
  
  target 'VoiceBarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VoiceBarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
